node_modules/
.next/
out/
.env
.env.local
*.log
.DS_Store

tsconfig.tsbuildinfo

# Stray installers / binaries that must never be committed
*.exe

# Docs and reports stay local — never publish to the repo
*.md
!README.md
# PDFs stay local too, except the core reference
*.pdf
!Element TD Engine.pdf

# Real-game reference captures — local evidence, not shipped
archive games/

# Per-machine Claude Code config: launch ports and local permissions
.claude/
